//concat

var girls = ['Cecilie', 'Lone'];
var boys = ['Emil', 'Tobias', 'Linus'];
var children = girls.concat(boys)

var cats = ['Larry', 'Zoe'];
var dogs = ['Xena', 'Lily'];
var pets = cats.concat(dogs)

var fruits = ['banana', 'watermelon', 'mango', 'orange', 'apple', 'kiwi'];
fruits.sort()

var to_do = ['cook dinner', 'clean kitchen', 'write', 'practice asl', 'practice hebrew', 'organize self for next week',]
to_do.sort()

